<div id="cabecera" class="zona">
    <nav id="selectorIdioma">
            
        {% if app.user('userLogged') %}
            <p>Hola {{ app.user('userLogged').username }}, <a href="{{path('app_logout')}}">Salir</a></p>
        {% else %}
            <p><a href="{{ path('app_login') }}">Iniciar sesión</a></p>
        {% endif %}
        
        <a href="#">Español</a>
        <a href="#">Inglés</a>
    </nav>
    <header>
        <img src="{{ asset('img/logo.jpeg') }}" alt="Logo página" width="150">
        <div>
            <h1>Taller de HTML5</h1>
            <h2>El mejor del mundo mundial</h2>
        </div>
    </header>
</div>